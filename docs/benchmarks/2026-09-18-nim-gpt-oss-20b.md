# Rewind benchmark

Model: `openai/gpt-oss-20b`

| strategy | accuracy | correct | questions | controls_passed | input_tokens | output_tokens | cache_read_tokens | total_tokens | cost_usd | tokens_per_correct | compactions | recall_hits | model_calls |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| plain | 0.625 | 5 | 8 | 1/2 | 119591 | 4147 | 0 | 123738 |  | 24748 | 2 | 0 | 48 |
| rewind | 0.875 | 7 | 8 | 2/2 | 173287 | 4584 | 0 | 177871 |  | 25410 | 2 | 10 | 58 |
| rewind@4000 | 0.875 | 7 | 8 | 1/2 | 144644 | 9979 | 0 | 154623 |  | 22089 | 10 | 11 | 66 |

Control questions ask about things never discussed; they pass when the answer says so (a keyword heuristic, so read the answers in results.json).

## Answers

### billing-service · plain · run 1

- **FAIL** (question) Write out the exact compute_late_fee function I gave you earlier.
  > I don’t have the exact `compute_late_fee` function definition available in this chat history. Could you please paste the original code so I can reproduce it exactly as you requested?
- **PASS** (question) What are our API rate limit and burst allowance? Just the numbers.
  > 1237   85
- **PASS** (question) Who is the release manager?
  > Priya Raman
- **PASS** (question) Why did we choose PostgreSQL over MongoDB?
  > Because we needed **row‑level tenant isolation**.   - PostgreSQL’s built‑in row‑level security (RLS) lets us enforce tenant boundaries directly in the database.   - It provides strong ACID guarantees, foreign‑key integrity, and transactional support that MongoDB lacks for these use cases.   - The re
- **FAIL** (control) What did we decide about the name of the Kubernetes cluster?
  > It will be named after an apartment building in Toronto.

### billing-service · rewind · run 1

- **PASS** (question) Write out the exact compute_late_fee function I gave you earlier.
  > ```python def compute_late_fee(days_overdue: int, daily_rate: float = 0.75) -> float:     grace_days = 3     billable = max(0, days_overdue - grace_days)     return round(min(billable * daily_rate, 30.0), 2) ```
- **PASS** (question) What are our API rate limit and burst allowance? Just the numbers.
  > 1237, 85
- **PASS** (question) Who is the release manager?
  > Priya Raman.
- **PASS** (question) Why did we choose PostgreSQL over MongoDB?
  > Because we need row‑level security for tenant isolation, PostgreSQL was chosen over MongoDB.
- **PASS** (control) What did we decide about the name of the Kubernetes cluster?
  > I don’t have a record of a decision about the Kubernetes cluster’s name in the conversation history. If you need help choosing or recalling the name, let me know!

### billing-service · rewind@4000 · run 1

- **PASS** (question) Write out the exact compute_late_fee function I gave you earlier.
  > ```python def compute_late_fee(days_overdue: int, daily_rate: float = 0.75) -> float:     grace_days = 3     billable = max(0, days_overdue - grace_days)     return round(min(billable * daily_rate, 30.0), 2) ```
- **PASS** (question) What are our API rate limit and burst allowance? Just the numbers.
  > 1237 requests/minute, 85 burst allowance.
- **PASS** (question) Who is the release manager?
  > Priya Raman.
- **PASS** (question) Why did we choose PostgreSQL over MongoDB?
  > Because we need **row‑level security** to isolate tenants, which PostgreSQL supports but MongoDB does not.
- **PASS** (control) What did we decide about the name of the Kubernetes cluster?
  > I don’t have a record of a decision on the cluster name in the archived notes. If you’d like to settle on one, let me know the preferred format or any constraints you have, and I can help draft a suitable name.

### mobile-app · plain · run 1

- **FAIL** (question) Show me the exact normalize_sku function from earlier.
  > I’m sorry, but I don’t have a record of the `normalize_sku` function that was mentioned earlier. If you can share the code or any details about how it’s implemented, I’ll be happy to help you review, modify, or explain it.
- **PASS** (question) What are the maximum upload size and the thumbnail width?
  > - **Maximum upload size**: **48 MB**   - **Thumbnail width**: **312 px**
- **PASS** (question) Who is the design lead?
  > The design lead for the mobile app is **Amara Okafor**.
- **FAIL** (question) Which version ships offline mode, and why?
  > **Offline mode will ship in v2.3**.   This update is being placed in that release because many field‑agent users rely on the app when they have spotty or no connectivity—adding offline support is a priority for ensuring the app remains usable in those environments.
- **PASS** (control) What budget did we approve for the TV advertising campaign?
  > I don’t have any record of a TV advertising campaign budget in the information that’s been shared. If you can point me to the notes or a document where that budget was discussed, I’ll gladly pull it out for you.

### mobile-app · rewind · run 1

- **PASS** (question) Show me the exact normalize_sku function from earlier.
  > ```python def normalize_sku(raw: str) -> str:     cleaned = raw.strip().upper().replace(" ", "-")     return cleaned[:14] if len(cleaned) > 14 else cleaned.ljust(6, "0") ```
- **PASS** (question) What are the maximum upload size and the thumbnail width?
  > - **Maximum upload size:** 48 MB   - **Thumbnail width:** 312 px (per side)
- **PASS** (question) Who is the design lead?
  > Amara Okafor.
- **FAIL** (question) Which version ships offline mode, and why?
  > 
- **PASS** (control) What budget did we approve for the TV advertising campaign?
  > We haven’t set a specific budget for the TV advertising campaign yet. No figure has been approved or committed in the discussions you’ve shared.

### mobile-app · rewind@4000 · run 1

- **PASS** (question) Show me the exact normalize_sku function from earlier.
  > ```python def normalize_sku(raw: str) -> str:     cleaned = raw.strip().upper().replace(" ", "-")     return cleaned[:14] if len(cleaned) > 14 else cleaned.ljust(6, "0") ```
- **PASS** (question) What are the maximum upload size and the thumbnail width?
  > The maximum upload size is **48 MB** and the thumbnail width is **312 px**.
- **FAIL** (question) Who is the design lead?
  > 
- **PASS** (question) Which version ships offline mode, and why?
  > Offline mode ships in **v2.3** – because field agents lose signal on site, and v2.3 will enable them to use the app in offline mode.
- **FAIL** (control) What budget did we approve for the TV advertising campaign?
  > There was no TV‑advertising campaign budget discussed or approved in our records. If you need a number, we’ll have to capture it in a new budget request.
